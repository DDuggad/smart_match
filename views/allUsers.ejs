<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Applicant Profiles</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f0f2f0, #e0e5e0);
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 12px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
            padding: 30px;
            width: 85%;
            max-width: 960px;
            backdrop-filter: blur(10px);
        }

        header {
            text-align: center;
            margin-bottom: 30px;
        }

        h1 {
            color: #007bff;
            margin-bottom: 15px;
        }

        .applicant-list {
            list-style: none;
            padding: 0;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
        }
        #add-listing-form {
            display: flex; /* Enable flexbox for centering */
            justify-content: center; /* Center the button horizontally */
            margin-bottom: 20px;
        }

        #add-listing-form button {
            background-color: #005408;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
            white-space: nowrap; /* Prevent text wrapping on smaller screens */
        }

        #add-listing-form button:hover {
            background-color: #007bff;
        }

        .applicant-card {
            background-color: #fff;
            border-radius: 8px;
            padding: 20px;
            border: 1px solid #ddd;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        }

        .applicant-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 18px rgba(0, 0, 0, 0.18);
        }

        .applicant-name {
            color: #28a745;
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 1.1rem;
        }

        .applicant-details p {
            margin-bottom: 8px;
            font-size: 0.85rem;
            color: #555;
        }

        .applicant-details strong {
            font-weight: bold;
            color: #333;
        }

        .applicant-skills {
            color: #0056b3;
        }

        .no-applicants {
            text-align: center;
            color: #777;
            font-style: italic;
        }

        footer {
            text-align: center;
            margin-top: 30px;
            color: #777;
            font-size: 0.8rem;
        }

        /* Background Pattern */
        body::before {
            content: "";
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('data:image/svg+xml,%3Csvg width="42" height="42" viewBox="0 0 42 42" xmlns="http://www.w3.org/2000/svg"%3E%3Cg id="Page-1" fill="none" fill-rule="evenodd"%3E%3Cg id="noise" fill="%23e3e3e3" fill-opacity="0.08"%3E%3Cpath d="M0 0h42v42H0z"/%3E%3Cpath d="M6.75 8.25c0-.966.783-1.75 1.75-1.75.966 0 1.75.784 1.75 1.75 0 .966-.784 1.75-1.75 1.75-.967 0-1.75-.784-1.75-1.75zm19.5 10c0-.966.784-1.75 1.75-1.75.966 0 1.75.784 1.75 1.75 0 .966-.784 1.75-1.75 1.75-.967 0-1.75-.784-1.75-1.75zm-13 14.5c0-.967.783-1.75 1.75-1.75.967 0 1.75.783 1.75 1.75 0 .967-.783 1.75-1.75 1.75-.967 0-1.75-.783-1.75-1.75zm16.5-21.5c0-.966.784-1.75 1.75-1.75.966 0 1.75.784 1.75 1.75 0 .966-.784 1.75-1.75 1.75-.967 0-1.75-.784-1.75-1.75zm-23 15c0-.967.783-1.75 1.75-1.75.967 0 1.75.783 1.75 1.75 0 .967-.783 1.75-1.75 1.75-.967 0-1.75-.783-1.75-1.75zm26.5-11.5c0-.966.784-1.75 1.75-1.75.966 0 1.75.784 1.75 1.75 0 .966-.784 1.75-1.75 1.75-.967 0-1.75-.784-1.75-1.75zM11.5 26.5c0-.967.783-1.75 1.75-1.75.967 0 1.75.783 1.75 1.75 0 .967-.783 1.75-1.75 1.75-.967 0-1.75-.783-1.75-1.75zm18-18c0-.966.784-1.75 1.75-1.75.966 0 1.75.784 1.75 1.75 0 .966-.784 1.75-1.75 1.75-.967 0-1.75-.784-1.75-1.75zM17 37c0-.967.783-1.75 1.75-1.75.967 0 1.75.783 1.75 1.75 0 .967-.783 1.75-1.75 1.75-.967 0-1.75-.783-1.75-1.75z"/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');
            opacity: 0.8;
            z-index: -1;
        }
        .applicant-details > form {
            display: flex;
            justify-content: center;
        }
        .applicant-details > form button{
            margin-left: 0; /* Remove any potential left margin */
        }
        
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous">
</head>
<body>
    <div class="container">
       
        <form id="add-listing-form" method="get" action="/home">
            <button type="submit">Home</button>
        </form>
        <header>
            <h1>Applicant Profiles</h1>
        </header>
        <form id="add-listing-form" method="get" action="/users/new">
            <button type="submit">Add New Applicant</button>
        </form>
        
        <main>
            <ul class="applicant-list">
                <% if (users && users.length > 0) { %>
                    <% users.forEach(user => { %>
                        <li class="applicant-card" data-applicant-id="<%= user.applicant_id %>">
                            <h2 class="applicant-name"><%= user.name %></h2>
                            <div class="applicant-details">
                               
                                <p><strong>Resume:</strong> <span class="applicant-resume"><%= user.resume %></span></p>
                                <p><strong>Skills:</strong> <span class="applicant-skills"><%= user.skills %></span></p>
                                <p><strong>Experience:</strong> <span class="applicant-experience"><%= user.experience %></span></p>
                                <p><strong>Education:</strong> <span class="applicant-education"><%= user.education %></span></p>
                                <form method="get" , action="/users/<%=user.applicant_id%>">
                                    <button class="btn btn-secondary">Suitable Companies</button>
                                </form>
                            </div>
                        </li>
                    <% }); %>
                <% } else { %>
                    <li class="no-applicants">No applicants found.</li>
                <% } %>
            </ul>
        </main>
        <footer>
            <p> Subscribe to INFINICERS.</p>
        </footer>
    </div>

    <script>
        // Basic JavaScript for potential interactivity (no "View Details" button now)
        document.addEventListener('DOMContentLoaded', () => {
            const applicantCards = document.querySelectorAll('.applicant-card');
            applicantCards.forEach(card => {
                card.addEventListener('mouseenter', () => {
                    card.style.boxShadow = '0 8px 20px rgba(0, 0, 0, 0.2)';
                    card.style.transform = 'translateY(-3px)';
                });
                card.addEventListener('mouseleave', () => {
                    card.style.boxShadow = '0 2px 8px rgba(0, 0, 0, 0.1)';
                    card.style.transform = 'translateY(0)';
                });
            });
        });

    </script>
</body>
</html>