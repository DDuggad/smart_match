<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JobHunt</title>
    <style>
        /* General body styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f8ff;
            /* Light blue background */
            text-align: center;
            /* Center all text */
            padding: 0;
            margin: 0;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            /* Full viewport height */
        }

        /* Header styling */
        h1 {
            color: #2c3e50;
            /* Dark blue color */
            font-size: 3em;
            /* Larger and more eye-catching */
            margin-top: 20px;
        }

        /* Paragraph styling */
        p {
            font-size: 5.8em;
            color: #000000;
            /* Teal color */
            margin-top: 20px;
        }

        /* Image styling */
        img {
            position: absolute;
            top: 20px;
            right: 20px;
            width: 150px;
            /* Adjust image size */
            height: auto;
        }

        /* Button container */
        .button-container {
            margin-top: 30px;
        }

        /* Style for buttons */
        .button {
            padding: 15px 40px;
            font-size: 1.5em;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin: 10px;
            width: 250px;
            /* Width for the buttons */
        }

        /* Apply Now button style */
        .apply-now {
            background-color: #c2d721;
            /* Light orange */
        }

        /* Hiring button style */
        .hiring {
            background-color: #258fbd;
            /* Light green */
        }

        /* Footer style */
        footer {
            margin-top: 40px;
            font-size: 1.2em;
            color: #333;
        }

        /* Keep Shining text */
        p.heart {
            font-size: 1.5em;
            color: #e74c3c;
        }
    </style>
</head>

<body>
    <!-- Logo Image - Updated path to correctly reference the logo in views folder -->
    <img src="./treeeeeeeeeeee.png" alt="JobHunt Logo">

    <!-- Main Content -->
    <p><b>JobHunt</b></p>
    <h1>Start your professional journey today!!!</h1>

    <!-- Button Container -->
    <div class="button-container">
        <form method="get" , action="/users">
            <button class="button apply-now">For Candidates</button>
        </form>
        <form method="get" action="/listings">
            <button class="button hiring">For Employers</button>
        </form>
    </div>

    <!-- Footer -->
    <footer>Thank you for visiting our Website</footer>

    <!-- Keep Shining message -->
    <p class="heart">Keep Shining &hearts;</p>
</body>

</html>